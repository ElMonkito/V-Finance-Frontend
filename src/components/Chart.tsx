import MobileNav from "./MobileNav";
import {Box} from "@mui/material"

export default function Chart() {
    return (
        <Box>
        <h1>Graph</h1>
        <MobileNav />
        </Box>
    );
};